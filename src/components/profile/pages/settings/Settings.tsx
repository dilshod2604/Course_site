import React from "react";
import scss from "./Settings.module.scss";
const Settings = () => {
  return (
    <section className={scss.Settings}>
      <div className={scss.welcome_content}>Settings</div>
    </section>
  );
};

export default Settings;
